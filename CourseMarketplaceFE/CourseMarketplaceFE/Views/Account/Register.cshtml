@using LinkedLearn.Models.UserVM
@model RegisterViewModel
@{
    ViewData["Title"] = "Register";
}

<main class="flex-grow flex items-center justify-center pt-24 pb-12 px-6 bg-login-gradient relative">
    <div class="w-full max-w-md bg-white p-10 rounded-2xl shadow-xl border border-slate-100 relative z-10">
        <div class="text-center mb-8">
            <h2 class="text-3xl font-headline font-extrabold text-on-surface mb-2">Create Account</h2>
            <p class="text-slate-500 text-sm">Join LinkedLearn today and start learning.</p>


        </div>

        <form asp-controller="Account" asp-action="Register" asp-route-giftToken="@ViewBag.GiftToken" asp-route-returnUrl="@ViewBag.ReturnUrl" method="post" class="space-y-4">

            @* FullName *@
            <div>
                <label asp-for="FullName" class="block text-sm font-semibold text-on-surface mb-1.5">Full Name</label>
                <input asp-for="FullName"
                       class="w-full px-4 py-3 bg-slate-50 border border-transparent rounded-lg focus:bg-white focus:border-primary/30 focus:ring-4 focus:ring-primary/10 outline-none transition-all"
                       placeholder="Nguyen Van A" autofocus tabindex="1" />
                <span asp-validation-for="FullName" class="text-[11px] text-red-500 mt-1 block font-medium"></span>
            </div>

            @* Email *@
            <div>
                <label asp-for="Email" class="block text-sm font-semibold text-on-surface mb-1.5">Email Address</label>
                <input asp-for="Email"
                       class="w-full px-4 py-3 bg-slate-50 border border-transparent rounded-lg focus:bg-white focus:border-primary/30 focus:ring-4 focus:ring-primary/10 outline-none transition-all"
                       placeholder="name@example.com" tabindex="2" />
                <span asp-validation-for="Email" class="text-[11px] text-red-500 mt-1 block font-medium"></span>
            </div>

            @* Username *@
            <div>
                <label asp-for="Username" class="block text-sm font-semibold text-on-surface mb-1.5">Username</label>
                <input asp-for="Username"
                       class="w-full px-4 py-3 bg-slate-50 border border-transparent rounded-lg focus:bg-white focus:border-primary/30 focus:ring-4 focus:ring-primary/10 outline-none transition-all"
                       placeholder="username123" tabindex="3" />
                <span asp-validation-for="Username" class="text-[11px] text-red-500 mt-1 block font-medium"></span>
            </div>

            @* Password *@
            <div>
                <label asp-for="Password" class="block text-sm font-semibold text-on-surface mb-1.5">Password</label>
                <div class="relative">
                    <input asp-for="Password"
                           id="regPassword"
                           class="w-full pl-4 pr-12 py-3 bg-slate-50 border border-transparent rounded-lg focus:bg-white focus:border-primary/30 focus:ring-4 focus:ring-primary/10 outline-none transition-all"
                           placeholder="••••••••"
                           type="password" tabindex="4" />
                    <button type="button" tabindex="-1" onclick="togglePassword('regPassword', this)" class="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-primary transition-colors flex items-center">
                        <span class="material-symbols-outlined text-xl">visibility</span>
                    </button>
                </div>
                <span asp-validation-for="Password" class="text-[11px] text-red-500 mt-1 block font-medium"></span>
            </div>

            @* Confirm Password *@
            <div>
                <label asp-for="ConfirmPassword" class="block text-sm font-semibold text-on-surface mb-1.5">Confirm Password</label>
                <div class="relative">
                    <input asp-for="ConfirmPassword"
                           id="regConfirmPassword"
                           class="w-full pl-4 pr-12 py-3 bg-slate-50 border border-transparent rounded-lg focus:bg-white focus:border-primary/30 focus:ring-4 focus:ring-primary/10 outline-none transition-all"
                           placeholder="••••••••"
                           type="password" tabindex="5" />
                    <button type="button" tabindex="-1" onclick="togglePassword('regConfirmPassword', this)" class="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-primary transition-colors flex items-center">
                        <span class="material-symbols-outlined text-xl">visibility</span>
                    </button>
                </div>
                <span asp-validation-for="ConfirmPassword" class="text-[11px] text-red-500 mt-1 block font-medium"></span>
            </div>

            <button tabindex="6" class="w-full bg-primary text-white font-headline font-bold py-4 rounded-xl shadow-lg shadow-primary/20 hover:scale-[1.02] active:scale-95 transition-all mt-4"
                    type="submit">
                Create Account
            </button>
        </form>

        <p class="text-center text-sm text-slate-500 mt-8">
            Already have an account? <a href="/Account/Login" class="text-primary font-bold hover:underline">Log in</a>
        </p>
    </div>
</main>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function togglePassword(inputId, btn) {
            const input = document.getElementById(inputId);
            const icon = btn.querySelector('.material-symbols-outlined');
            if (input.type === 'password') {
                input.type = 'text';
                icon.textContent = 'visibility_off';
            } else {
                input.type = 'password';
                icon.textContent = 'visibility';
            }
        }
    </script>
}