@{
    ViewData["Title"] = "Đổi mật khẩu";
}

<div class="max-w-7xl mx-auto px-6 lg:px-12 py-10">
    <div class="flex flex-col lg:flex-row gap-8">
        @* Sidebar *@
        <aside class="w-full lg:w-72 flex-shrink-0">
            <div class="bg-white rounded-[2rem] p-6 shadow-sm border border-slate-100 sticky top-24">
                <nav class="flex flex-col gap-2">
                    <a href="/Account/Profile" class="flex items-center gap-4 px-4 py-3 rounded-2xl text-slate-500 hover:bg-slate-50 transition-all">
                        <span class="material-symbols-outlined">person</span>
                        <span>Profile</span>
                    </a>
                    <a href="/Account/EditProfile" class="flex items-center gap-4 px-4 py-3 rounded-2xl text-slate-500 hover:bg-slate-50 transition-all">
                        <span class="material-symbols-outlined">edit_square</span>
                        <span>Edit Profile</span>
                    </a>
                    <a href="/Account/ChangePassword" class="flex items-center gap-4 px-4 py-3 rounded-2xl bg-primary-container/30 text-primary font-bold transition-all mt-2 border-t border-slate-100 pt-4">
                        <span class="material-symbols-outlined" style='font-variation-settings: "FILL" 1;'>lock</span>
                        <span>Đổi mật khẩu</span>
                    </a>
                </nav>
            </div>
        </aside>

        @* Main Content *@
        <div class="flex-grow space-y-8">
            <div class="mb-4">
                <h1 class="text-3xl font-extrabold text-slate-800">Đổi mật khẩu</h1>
                <p class="text-slate-500">Cập nhật mật khẩu để bảo vệ tài khoản của bạn.</p>
            </div>

            <section class="bg-white rounded-[2.5rem] p-8 md:p-10 shadow-sm border border-slate-100 max-w-2xl">
                @if (ViewBag.ErrorMessage != null)
                {
                    <div class="bg-red-50 text-red-500 px-6 py-4 rounded-2xl font-medium flex items-center gap-3 mb-6">
                        <span class="material-symbols-outlined">error</span>
                        @ViewBag.ErrorMessage
                    </div>
                }

                <form action="/Account/ChangePassword" method="post" class="space-y-6" id="changePasswordForm">
                    <div class="space-y-2">
                        <label class="text-xs font-bold uppercase text-slate-400 ml-1">Mật khẩu hiện tại</label>
                        <div class="relative">
                            <span class="absolute left-5 top-1/2 -translate-y-1/2 material-symbols-outlined text-slate-400 text-lg">password</span>
                            <input type="password" name="currentPassword" required class="w-full bg-slate-50 border-none rounded-2xl pl-12 pr-5 py-4 focus:ring-2 focus:ring-primary/20 transition-all" placeholder="Nhập mật khẩu hiện tại" />
                        </div>
                    </div>

                    <div class="space-y-2">
                        <label class="text-xs font-bold uppercase text-slate-400 ml-1">Mật khẩu mới</label>
                        <div class="relative">
                            <span class="absolute left-5 top-1/2 -translate-y-1/2 material-symbols-outlined text-slate-400 text-lg">lock</span>
                            <input type="password" name="newPassword" id="newPwd" required minlength="6" class="w-full bg-slate-50 border-none rounded-2xl pl-12 pr-5 py-4 focus:ring-2 focus:ring-primary/20 transition-all" placeholder="Nhập mật khẩu mới (tối thiểu 6 ký tự)" />
                        </div>
                    </div>

                    <div class="space-y-2">
                        <label class="text-xs font-bold uppercase text-slate-400 ml-1">Xác nhận mật khẩu mới</label>
                        <div class="relative">
                            <span class="absolute left-5 top-1/2 -translate-y-1/2 material-symbols-outlined text-slate-400 text-lg">lock_check</span>
                            <input type="password" name="confirmNewPassword" id="confirmPwd" required minlength="6" class="w-full bg-slate-50 border-none rounded-2xl pl-12 pr-5 py-4 focus:ring-2 focus:ring-primary/20 transition-all" placeholder="Nhập lại mật khẩu mới" />
                        </div>
                        <p id="pwdError" class="text-xs text-red-500 font-medium ml-1 hidden mt-1">Mật khẩu xác nhận không khớp!</p>
                    </div>

                    <div class="pt-6">
                        <button type="submit" class="px-10 py-4 w-full md:w-auto bg-primary text-white font-bold rounded-2xl shadow-lg shadow-primary/20 hover:opacity-90 active:scale-95 transition-all">
                            Cập nhật mật khẩu
                        </button>
                    </div>
                </form>
            </section>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('changePasswordForm').addEventListener('submit', function(e) {
            const newPwd = document.getElementById('newPwd').value;
            const confirmPwd = document.getElementById('confirmPwd').value;
            const errorMsg = document.getElementById('pwdError');
            
            if (newPwd !== confirmPwd) {
                e.preventDefault();
                errorMsg.classList.remove('hidden');
                document.getElementById('confirmPwd').classList.add('ring-2', 'ring-red-500/50');
            } else {
                errorMsg.classList.add('hidden');
                document.getElementById('confirmPwd').classList.remove('ring-2', 'ring-red-500/50');
            }
        });
    </script>
}
