@startuml view_lesson_review_reports
<style>
    sequenceDiagram{
        actor{
            BackgroundColor white
            LineThickness 1.0
        }
        participant{
            BackgroundColor white
            RoundCorner 0
        }
        database{
            BackgroundColor white
        }
        groupHeader{
            BackgroundColor white
        }
        referenceHeader{
            BackgroundColor white
        }
        reference{
            BackgroundColor white
        }
    }
</style>

hide footbox

mainframe View List of Lesson Review Reports
actor Staff as act

participant "LessonReviewReportListView" as view
participant "AdminModerationController" as be_ctrl
participant ":ReportService" as sv
participant ":ReportRepository" as repo
participant ":AppDbContext" as ctx

database Database as db

act++
act->view++: Access "Lesson Review Reports" page
view->be_ctrl++: GET /admin/moderation/reports/lesson-reviews?page={page}&pageSize={size}
be_ctrl->sv++: GetAllLessonReviewReportsAsync(status, page, pageSize)
sv->repo++: GetAllLessonReviewReportsAsync(status, page, pageSize)
repo->ctx++: Query LessonReviewReport entities
ctx->db++: Execute SQL query
db-->>ctx: Return result set
ctx-->>repo: (reports, totalCount)
repo-->>sv: (reports, totalCount)
sv-->>be_ctrl: PagedResult<LessonReviewReportDetailResponse>
be_ctrl-->>view: JSON payload
view-->>act: Render list UI
act--
@enduml
