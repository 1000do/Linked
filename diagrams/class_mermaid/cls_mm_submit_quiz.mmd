classDiagram
direction LR

%% ==================== FRONTEND ====================

class QuizAttemptControllerFE["QuizAttemptController"] {
    <<frontend controller>>
    + SubmitQuiz(request: SubmitQuizViewModel) Task~IActionResult~
}

%% ==================== BACKEND - CONTROLLER ====================

class QuizAttemptControllerBE["QuizAttemptController"] {
    <<backend controller>>
    - _quizService : IQuizService
    
    + SubmitAttempt(request: QuizAttemptSubmitRequest) Task~IActionResult~
}

%% ==================== BACKEND - DTOs ====================

class QuizAttemptSubmitRequest {
    <<dto>>
    + QuizId : int
    + TimeTakenMinutes : int
    + Answers : List~QuizAnswerSubmitRequest~
}

class QuizAnswerSubmitRequest {
    <<dto>>
    + QuestionId : int
    + SelectedOptionIds : List~int~
    + TextAnswer : string?
}

class QuizAttemptResultResponse {
    <<dto>>
    + AttemptId : int
    + TotalScore : decimal
    + IsPassed : bool
    + CorrectCount : int
    + IncorrectCount : int
}

%% ==================== BACKEND - DOMAIN ENTITIES ====================

class QuizAttempt {
    <<entity>>
    + AttemptId : int
    + QuizId : int
    + UserId : int
    + AttemptDate : DateTime?
    + TotalScore : decimal
    + IsPassed : bool
    + TimeTakenMinutes : int?
    + AttemptDetails : ICollection~QuizAttemptDetail~
}

class QuizAttemptDetail {
    <<entity>>
    + DetailId : int
    + AttemptId : int
    + QuestionId : int
    + SelectedOptionId : int?
    + TextAnswer : string?
    + IsCorrect : bool?
    + PointsEarned : decimal?
}

%% ==================== BACKEND - SERVICE INTERFACES ====================

class IQuizService {
    <<interface>>
    + SubmitAttemptAsync(request: QuizAttemptSubmitRequest, userId: int) Task~QuizAttemptResultResponse~ *
}

class IQuizRepository {
    <<interface>>
    + GetQuizWithQuestionsAsync(quizId: int) Task~Quiz?~ *
    + AddAttemptAsync(attempt: QuizAttempt) void *
    + SaveChangesAsync() Task~int~ *
}

%% ==================== BACKEND - SERVICE IMPLEMENTATION ====================

class QuizService {
    - _quizRepo : IQuizRepository
    
    + SubmitAttemptAsync(request: QuizAttemptSubmitRequest, userId: int) Task~QuizAttemptResultResponse~
}

%% ==================== BACKEND - REPOSITORY IMPLEMENTATION ====================

class QuizRepository {
    - _context : AppDbContext
    
    + GetQuizWithQuestionsAsync(quizId: int) Task~Quiz?~
    + AddAttemptAsync(attempt: QuizAttempt) void
    + SaveChangesAsync() Task~int~
}

%% ==================== BACKEND - DbContext ====================

class AppDbContext {
    + QuizAttempts : DbSet~QuizAttempt~
    + QuizAttemptDetails : DbSet~QuizAttemptDetail~
}

%% ==================== RELATIONSHIPS ====================

%% === AGGREGATION & COMPOSITION ===
QuizAttemptSubmitRequest *-- QuizAnswerSubmitRequest
QuizAttempt *-- QuizAttemptDetail

%% === UNIDIRECTIONAL ASSOCIATION ===
QuizAttemptControllerBE --> IQuizService
QuizService --> IQuizRepository
QuizRepository --> AppDbContext

%% === INTERFACE REALIZATION ===
QuizService ..|> IQuizService
QuizRepository ..|> IQuizRepository

%% === DEPENDENCY ===
QuizAttemptControllerFE ..> QuizAttemptControllerBE
QuizAttemptControllerBE ..> QuizAttemptSubmitRequest
QuizAttemptControllerBE ..> QuizAttemptResultResponse
QuizService ..> QuizAttemptSubmitRequest
QuizService ..> QuizAttemptResultResponse
QuizService ..> QuizAttempt
QuizRepository ..> QuizAttempt

%% ==================== STYLES ====================

classDef default fill:#FFFFFF,stroke:#000000,stroke-width:1px,color:#000000